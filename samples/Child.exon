// inherit from Base.exon and add new properties

Base {
    timeout: 30
    retryAttempts: 3

    // load internals/Database.exon
    database: internals.Database {
        name: "appdb"
        poolSize: 20
    }
}
