// inherit from Child.exon and override existing properties

Child {
    port: 3030
    timeout: 15

    // partial object override
    database: _ {
        name: "mydb"
        poolSize: 50
    }
}
