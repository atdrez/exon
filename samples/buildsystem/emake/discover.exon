using fn.*

wrapper {
    dir: ""
    path: ""
    content: string.split {
        string.trim {
            process.exec { "find " @root.dir " -name '" @root.path "' | sort" }
        }
        separator: "\n"
    }
}