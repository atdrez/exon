fn.js.eval {
    __content__: []

    content: """
    const c = $__content__;
    if (c.length < 2)
        throw new Error("Invalid number of arguments");

    let r = c[0];
    for (let i = 1; i < c.length; i++)
        r ||= c[i];
    return r;
    """
}