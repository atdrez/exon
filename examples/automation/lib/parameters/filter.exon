using fn.*

foreach {
    type: ""
    do: sequence@seq {
        v: parameter{}
        if {
            condition: get { target: @seq.v property: @root.type }
            then: sequence {
                del { target: @seq.v property: @root.type }
                @seq.v
            }
        }
    }
}