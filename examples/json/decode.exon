{
    // evaluate json string with params and return the object
    endpoint: fn.json.decode {
        content: "{\"url\": \"https://$host/$version\", \"timeout\": $timeout}"

        host: "api.example.com"
        version: "v2"
        timeout: 30
    }
}