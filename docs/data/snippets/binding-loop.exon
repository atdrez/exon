{
    items: ["alpha", "beta", "gamma"]

    result: fn.foreach {
        data: @root.items
        do: Object@item {
            value: fn.parameter{}
            upper: fn.string.join { "[" @item.value "]" }
        }
    }
}